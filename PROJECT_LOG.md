# 문서방 프로젝트 로그

## 문서 우선 원칙

새로운 작업자는 먼저 `MANIFESTO.md`를 읽고, 다음으로 이 `PROJECT_LOG.md`를 읽은 뒤 코드를 분석한다. 코드는 철학을 구현하는 수단이며, 프로젝트 문서는 프로젝트의 기억이다.

## 2026-07-05

### 오늘 구현한 기능

- 문서방 MVP 첫 화면을 정상 한글 UI로 복구했다.
- 검색을 `src/data/tools.ts`의 프론트엔드 정적 데이터 필터링으로 정리했다.
- 검색 결과 카드를 제목, 분류, 한 줄 답, 설명, 신뢰 라벨, 제공 기능, 근거, 열기 버튼 구조로 정리했다.
- 모바일에서 카드가 한 열로 안정적으로 보이도록 CSS를 정리했다.
- GitHub Pages 하위 경로 배포를 고려해 Vite `base: './'` 설정과 favicon 상대 경로를 반영했다.
- 프로젝트 문서 4종을 생성 또는 최신화했다.

### 오늘 결정한 정책

- MVP는 정적 웹으로 유지한다.
- 로그인, 회원가입, DB, 게시판, 커뮤니티 기능은 넣지 않는다.
- 사용자 문서와 업로드 파일은 서버에 저장하지 않는다.
- 결과물은 사용자의 PC에 저장하는 방향을 기본 정책으로 삼는다.
- 검색은 AI 검색이 아니라 검증된 답안 카드 검색이다.
- 공식자료, 법령, KS, KCS, KDS, 표준시장단가를 우선 근거로 삼는다.
- AI는 검색의 주체가 아니며, 향후 실정보고/분쟁검토의 보조 기능으로만 검토한다.
- 앞으로 작업자는 코드보다 `MANIFESTO.md`와 `PROJECT_LOG.md`를 먼저 읽는다.

### 수정한 내용

- 깨진 한글 인코딩과 JSX/TypeScript 문법 오류 가능성을 복구했다.
- `src/data/tools.ts`의 검색 데이터를 답안 카드 구조에 맞게 재작성했다.
- `Header`, `Hero`, `SearchHub`, `CategoryGrid`, `Principles`, `Workflow`, `Updates`, `Footer` 컴포넌트 문구를 프로젝트 철학에 맞게 정리했다.
- `src/App.css`에서 검색 카드, 신뢰 라벨, 메타 정보, 모바일 반응형을 개선했다.
- `README.md`, `MANIFESTO.md`, `PROJECT_LOG.md`, `ROADMAP.md`, `CHANGELOG.md`를 한국어 기준 문서로 정리했다.

### 발견한 문제

- 기존 코드와 문서의 한글이 깨져 있었고 일부 파일은 JSX/TypeScript 문법 자체가 깨질 수 있었다.
- 검색 결과가 검증된 답안 카드라기보다 관련 항목 목록처럼 흐를 위험이 있었다.
- GitHub Pages 하위 경로 배포 시 절대 경로 리소스가 깨질 수 있었다.
- PowerShell에서 `npm install`은 `npm.ps1` 실행 정책에 막혔고, `npm.cmd install`로 검증해야 했다.
- Google Drive 경로에서 `npm install` 실행 시 tar write/EBADF 경고가 발생했지만 종료 코드는 0이었다.

### 검증 결과

- `npm.cmd install`: 성공, 취약점 0개
- `npm.cmd run build`: 성공, TypeScript 빌드 및 Vite 빌드 통과
- `npm run dev`: Vite dev 서버 기동 확인

## 2026-07-08

### 오늘 구현한 기능 (디자인 시스템 수립)
- 문서방 공식 디자인 시스템 **MDS (Munseobang Design System)**을 최종 수립하고 명세화했다.
- 점진적 노출(Progressive Disclosure) 및 4단계 탐색(Explorer -> Task -> Workflow -> Workspace) 아키텍처를 정의하고 3가지 비주얼 UI 목업을 제작했다.
- MDS 표준을 명시한 `01_DESIGN_SYSTEM.md`부터 `06_HANDOVER_NOTES.md`까지 총 6종의 표준 기획 문서를 생성했다.
- 다음 단계 개발 에이전트(혹은 코더)를 위한 구현 착수 매핑 및 로컬 퍼스트 연동 전제 조건 정리를 완료했다.

## 2026-07-11

### 오늘 구현한 기능 (마이크로 포털 아키텍처 개편 및 런처 슬림화)
- **사이드바 제거 및 레이아웃 100% 확장**: 좌측 폴더 트리 탐색기를 제거하고 대시보드가 단독으로 화면 전체를 채우도록 레이아웃을 전면 리팩토링했습니다.
- **하위 카테고리 임베딩(iframe) 링크 해제**: 공사관리, 품질관리, 검수검측, 안전환경, 공무행정, 본사협업, 공통업무의 내부 iframe 임베딩 구조를 걷어냈습니다.
- **문서방 패밀리 앱 바로가기 그리드 신설**: 각 하위 실무 도구들을 독립된 주소(예: `./gongsailbo-pro/`, `./gumcheuk-pro/`, `./safety-environment-costs/` 및 외부 깃허브 배포 주소)로 연결해 새 창(`target="_blank"`)에서 가동하도록 구조화된 카드로 전면 배치했습니다.
- **건설기준 탐색기 모달 다이얼로그 연동**: 독립 주소가 없는 '건설기준 탐색기'는 포털 내에서 라이트박스 형태의 전체 화면 모달로 활성화되도록 구현했습니다.
- **배너 광고 및 제안 창 레이아웃 변경**: 기존 사이드바 하단 배너 영역을 대시보드 우하단 영역(뉴스 브리핑 옆)으로 옮겨 포털 메인 대시보드 하단 레이아웃을 고도화했습니다.
- **하위 앱 LocalStorage 접두사 적용 및 자동 마이그레이션**: 앱들 간 키 충돌 방지를 위해 접두사(`munseobang:`)를 적용하고, 기존 사용자의 데이터 유실을 방지하기 위해 구버전 키에서 신버전 키로 자동 데이터를 복제해주는 무중단 마이그레이션 호환성 코드를 탑재했습니다.

### 오늘 결정한 정책
- 하나의 모놀리식 프레임 안에서 수많은 iframe을 제어하던 종전 설계를 폐기하고, **"가벼운 단일 목적 웹페이지(Micro App) 개발 및 포털을 통한 새 탭 아웃링크 연결"** 구조를 문서방 핵심 설계 원칙으로 공식 채택했습니다.
- 이를 통해 초기 로딩 시 불필요한 브라우저 리소스 소모 및 UI 버벅거림을 근본적으로 해소하고, 유지보수 시 각 기능 간 결합도를 완전히 제거하여 안정성을 도모합니다.

### 수정한 내용
- `src/data/apps.ts`: `explorerFolders` 탐색기 구조를 뉴스브리핑 위주로 극소화하고, 대시보드 렌더링용 패밀리 앱 정보 `familyApps` 데이터 구조 추가
- `src/App.tsx`: 사이드바 렌더링 삭제, 대시보드 100% 넓이 스타일, 패밀리 앱 그리드 추가, 건설기준 탐색기 모달 연동 및 배너 하단 배치
- `src/App.css`: `.mds-os-container--full` 및 `.mds-main-content--full` 추가, 개별 웹 앱 카드 및 모달 창 애니메이션 스타일링
- `public/gongsailbo-pro/app.js`, `public/gumcheuk-pro/app.js`, `public/safety-environment-costs/app.js`: 고유 로컬스토리지 키 변경 및 구버전 키 자동 병합 마이그레이션 적용
- `tests/smoke.spec.ts`: 총 6개 항목(대시보드 레이아웃, 바로가기 확인, 모바일 반응형, 직접 URL 접속 및 새로고침 시 데이터 유지, JSON 파일 다운로드 검증)으로 구성된 Playwright 테스트 스크립트 작성

### 검증 및 배포 결과
- `npm.cmd run build`를 통한 컴파일 및 번들링 정상 동작 확인.
- `npx.cmd playwright test` 테스트 결과 **6개 시나리오 전원 정상 통과(6 passed)** 확인.
- 원격 저장소(`origin master`)의 최근 수집된 뉴스 크롤링 데이터 변경분과 안전하게 자동 병합(merge) 처리.
- `git push origin master` 수행하여 실서버(GitHub Pages) 자동 배포 파이프라인 가동 완료.

